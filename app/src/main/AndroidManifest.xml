<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.henley.simplecache.sample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CacheStringActivity" />
        <activity android:name=".CacheObjectActivity" />
        <activity android:name=".CacheJsonObjectActivity" />
        <activity android:name=".CacheJsonArrayActivity" />
        <activity android:name=".CacheBitmapActivity" />
        <activity android:name=".CacheDrawableActivity" />
        <activity android:name=".CacheStreamActivity" />

    </application>

</manifest>